<?php
/* @var $entity \Tcmed\Entity\Hold */
if($this->resultSet){
    $array = [];

    $array[] = [
        'menu_id'   => 'ID',
        'menu_nome' => 'Nome Fantasia'
    ];
    foreach ($this->resultSet as $entity) {
        $array[] = [
            'id'   => $entity->getId(),
            'nome' => $entity->getNome()
        ];
    }

    echo json_encode($array);
}else{
    echo '{}';
}
