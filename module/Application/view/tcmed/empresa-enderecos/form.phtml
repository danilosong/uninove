<h3><?php 
/**
 * @author
 */
echo $this->dataView['titulo']; 
$controller = 'empresaenderecos';
$route = 'tcmed/default';
?></h3>
<br />
<?php
/* @var $fh \Application\View\Helper\FormHelp*/
$fh = $this->formHelp($this,$this->form,$this->url($route,array('controller'=>$controller,'action'=>$this->dataView['action']),[],FALSE,$this->dataView['ajax']));
$fh->setHorizontal(true);
$fh->formInit('Cadastro Empresa Endereco:');
    $fh->openCol('12')->text('endPrincipal')->closeCol();
$fh->lineDown();
    $fh->openCol('12')->text('endTipo')->closeCol();
$fh->lineDown();
    $fh->openCol('12')->select('empresa')->closeCol();
$fh->lineDown();
    $fh->openCol('12')->select('endereco')->closeCol();
$fh->lineDown();
    $fh->openCol('12')->Submit('submit')->closeCol();
$fh->closeLine()->formEnd();
?>

<script lang="javascript">   
    
    function isValid(){
        return true;
    }
    
</script>
