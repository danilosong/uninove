<h3><?php echo $this->dataView['titulo']; 
$controller = 'histOcupacionals';
$route = 'tcmed/default';
?></h3>
<br />
<?php
/* @var $fh \Application\View\Helper\FormHelp
 * @author Allan Davini */
$fh = $this->formHelp($this,$this->form,$this->url($route,array('controller'=>$controller,'action'=>$this->dataView['action']),[],FALSE,$this->dataView['ajax']));
$fh->setHorizontal(true);
$fh->formInit('Cadastro de Historico Ocupacional:');
    $fh->openCol('12')->calend('dtIni')->closeCol();
$fh->lineDown();
    $fh->openCol('12')->calend('dtFim')->closeCol();
$fh->lineDown();
    $fh->openCol('12')->select('funcionario')->closeCol();
$fh->lineDown();
    $fh->openCol('12')->select('empresa')->closeCol();
$fh->lineDown();
    $fh->openCol('12')->select('administradora')->closeCol();
$fh->lineDown();
    $fh->openCol('12')->select('ocupacao')->closeCol();
$fh->lineDown();
    $fh->openCol('12')->select('status')->closeCol();
$fh->lineDown();
    $fh->openCol('12')->Submit('submit')->closeCol();
$fh->closeLine()->formEnd();
?>

<script lang="javascript">   
    
    function isValid(){
        return true;
    }
    
</script>
